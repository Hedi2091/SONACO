@{
    ViewData["Title"] = "Home";
}

<div class="container" style="height: 80vh; display: flex; align-items: center; justify-content: center;">
    <div class="row">
        <div class="col-md-4" style="margin-bottom: 40px;">
            <!-- Augmenter l'espace entre les boutons -->
            <a href="@Url.Action("Index", "Entree")" class="btn btn-primary btn-lg btn-block">Entree</a>
        </div>
        <div class="col-md-4" style="margin-bottom: 40px;">
            <!-- Augmenter l'espace entre les boutons -->
            <a href="@Url.Action("Index", "Lot")" class="btn btn-primary btn-lg btn-block">Lots</a>
        </div>
        <div class="col-md-4" style="margin-bottom: 40px;">
            <!-- Augmenter l'espace entre les boutons -->
            <a href="@Url.Action("Index", "Production")" class="btn btn-primary btn-lg btn-block">Production</a>
        </div>
    </div>
</div>
